/*
 * This file is generated by jOOQ.
 */
package com.verint.mpathy.db.live.tables;


import com.verint.mpathy.db.live.Live;
import com.verint.mpathy.db.live.tables.records.StruggleScoreSessionRecord;

import java.math.BigDecimal;

import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Identity;
import org.jooq.Name;
import org.jooq.Record;
import org.jooq.Row4;
import org.jooq.Schema;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * Holds the calculated struggle score for a specific session ID and its
 * references to relevant tables.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes" })
public class StruggleScoreSession extends TableImpl<StruggleScoreSessionRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>live.struggle_score_session</code>
     */
    public static final StruggleScoreSession STRUGGLE_SCORE_SESSION = new StruggleScoreSession();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<StruggleScoreSessionRecord> getRecordType() {
        return StruggleScoreSessionRecord.class;
    }

    /**
     * The column
     * <code>live.struggle_score_session.idstruggle_score_page</code>. The
     * primary key for identifying rows of this table.
     */
    public final TableField<StruggleScoreSessionRecord, Long> IDSTRUGGLE_SCORE_PAGE = createField(DSL.name("idstruggle_score_page"), SQLDataType.BIGINT.nullable(false).identity(true), this, "The primary key for identifying rows of this table.");

    /**
     * The column <code>live.struggle_score_session.idsite</code>. The FK for
     * reference to the ID column of the "site" table.
     */
    public final TableField<StruggleScoreSessionRecord, Integer> IDSITE = createField(DSL.name("idsite"), SQLDataType.INTEGER, this, "The FK for reference to the ID column of the \"site\" table.");

    /**
     * The column <code>live.struggle_score_session.idsession</code>. The FK for
     * reference to the ID column of the "session" table.
     */
    public final TableField<StruggleScoreSessionRecord, BigDecimal> IDSESSION = createField(DSL.name("idsession"), SQLDataType.NUMERIC, this, "The FK for reference to the ID column of the \"session\" table.");

    /**
     * The column <code>live.struggle_score_session.score</code>. The calculated
     * score for this session ID.
     */
    public final TableField<StruggleScoreSessionRecord, Long> SCORE = createField(DSL.name("score"), SQLDataType.BIGINT, this, "The calculated score for this session ID.");

    private StruggleScoreSession(Name alias, Table<StruggleScoreSessionRecord> aliased) {
        this(alias, aliased, null);
    }

    private StruggleScoreSession(Name alias, Table<StruggleScoreSessionRecord> aliased, Field<?>[] parameters) {
        super(alias, null, aliased, parameters, DSL.comment("Holds the calculated struggle score for a specific session ID and its references to relevant tables."), TableOptions.table());
    }

    /**
     * Create an aliased <code>live.struggle_score_session</code> table
     * reference
     */
    public StruggleScoreSession(String alias) {
        this(DSL.name(alias), STRUGGLE_SCORE_SESSION);
    }

    /**
     * Create an aliased <code>live.struggle_score_session</code> table
     * reference
     */
    public StruggleScoreSession(Name alias) {
        this(alias, STRUGGLE_SCORE_SESSION);
    }

    /**
     * Create a <code>live.struggle_score_session</code> table reference
     */
    public StruggleScoreSession() {
        this(DSL.name("struggle_score_session"), null);
    }

    public <O extends Record> StruggleScoreSession(Table<O> child, ForeignKey<O, StruggleScoreSessionRecord> key) {
        super(child, key, STRUGGLE_SCORE_SESSION);
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Live.LIVE;
    }

    @Override
    public Identity<StruggleScoreSessionRecord, Long> getIdentity() {
        return (Identity<StruggleScoreSessionRecord, Long>) super.getIdentity();
    }

    @Override
    public StruggleScoreSession as(String alias) {
        return new StruggleScoreSession(DSL.name(alias), this);
    }

    @Override
    public StruggleScoreSession as(Name alias) {
        return new StruggleScoreSession(alias, this);
    }

    /**
     * Rename this table
     */
    @Override
    public StruggleScoreSession rename(String name) {
        return new StruggleScoreSession(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public StruggleScoreSession rename(Name name) {
        return new StruggleScoreSession(name, null);
    }

    // -------------------------------------------------------------------------
    // Row4 type methods
    // -------------------------------------------------------------------------

    @Override
    public Row4<Long, Integer, BigDecimal, Long> fieldsRow() {
        return (Row4) super.fieldsRow();
    }
}
